name: Build APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Cache Gradle
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/viral-quiz/**/*.gradle*', '**/viral-quiz/**/gradle-wrapper.properties') }}
        
    - name: Generate Gradle Wrapper
      working-directory: ./viral-quiz
      run: gradle wrapper --gradle-version 8.2
        
    - name: Grant execute permission for gradlew
      working-directory: ./viral-quiz
      run: chmod +x gradlew
        
    - name: Build with Gradle
      working-directory: ./viral-quiz
      env:
        ANDROID_HOME: /usr/local/lib/android/sdk
      run: ./gradlew :app:assembleDebug
      
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: debug-apk
        path: viral-quiz/app/build/outputs/apk/debug/app-debug.apk
